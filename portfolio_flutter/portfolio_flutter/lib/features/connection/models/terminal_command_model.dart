class TerminalCommandModel {
  final String label;
  final String command;
  final String url;

  const TerminalCommandModel({
    required this.label,
    required this.command,
    required this.url,
  });
}