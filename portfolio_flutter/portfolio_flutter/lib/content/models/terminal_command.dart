class TerminalCommand {
  final String label;
  final String command;
  final String url;

  const TerminalCommand({
    required this.label,
    required this.command,
    required this.url,
  });
}
